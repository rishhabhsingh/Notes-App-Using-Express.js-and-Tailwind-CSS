<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Notes App(using backend)</title>
    <link rel="stylesheet" href="/stylesheets/styles.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <body class="bg-gradient-to-br from-indigo-50 to-purple-100 min-h-screen">

  <!-- 🔷 Header / Hero Section -->
  <header class="text-center py-10">
    <h1 class="text-4xl font-bold text-indigo-700">📝 My Notes</h1>
    <p class="text-gray-600 mt-2">Capture your thoughts beautifully</p>
  </header>

  <!-- 🧠 Main Container -->
  <main class="max-w-5xl mx-auto px-4">

    <!-- ✍️ Add Note Section -->
    <section class="bg-white rounded-xl shadow-lg p-6 mb-10 hover:shadow-xl transition">
  <h2 class="text-2xl font-semibold text-gray-800 mb-4">Add a New Note</h2>

  <form
    action="/notes"
    method="POST"
    class="space-y-4"
  >
    <input
      type="text"
      name="title"
      placeholder="Note Title"
      required
      class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-400"
    />

    <textarea
      name="content"
      rows="4"
      placeholder="Write your note here..."
      required
      class="w-full px-4 py-2 border rounded-lg resize-none focus:outline-none focus:ring-2 focus:ring-indigo-400"
    ></textarea>

    <button
      type="submit"
      class="bg-indigo-600 text-white px-6 py-2 rounded-lg hover:bg-indigo-700 transition"
    >
      Save Note
    </button>
  </form>
</section>

    <!-- 🗂 Notes Cards Section -->
    <section>
      <h2 class=" text-2xl font-semibold text-gray-800 mb-4">Your Notes</h2>

      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
  <% files.forEach(val => { %>
    <div class="bg-white p-5 rounded-xl shadow">
      <h3 class="font-bold text-lg">
        <%= val.replace('.txt','') %>
      </h3>

      <a href="/notes/<%= val.replace('.txt','') %>"
         class="text-indigo-600 underline">
        View Note
      </a>
    </div>
  <% }) %>
</div>


      </div>
    </section>
  </main>

  <!-- 🔻 Footer Section -->
  <footer class="text-center py-6 mt-10 text-gray-500">
    Made with 💜 using Tailwind CSS and Express.js!
  </footer>
<script src="/javascripts/main.js"></script>
</body>
</html>